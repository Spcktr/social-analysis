(ns social-analysis.core
  (:use [social_analysis.graph :as g]
        [social_analysis.ego :as ego]
        [social_analysis.utils :as utils])

(defn -main
  "I don't do a whole lot ... yet."
  [& args]
  (println "Hello, World!"))
